// Code generated by Wire. DO NOT EDIT.

//go:generate go run github.com/google/wire/cmd/wire
//go:build !wireinject
// +build !wireinject

package main

import (
	"github.com/go-kratos/kratos/v2"
	"github.com/go-kratos/kratos/v2/log"
	"mall-go/app/mall/admin/internal/biz"
	"mall-go/app/mall/admin/internal/conf"
	"mall-go/app/mall/admin/internal/data"
	"mall-go/app/mall/admin/internal/server"
	"mall-go/app/mall/admin/internal/service"
)

// Injectors from wire.go:

// initApp init kratos application.
func initApp(bootstrap *conf.Bootstrap, registry *conf.Registry, logger log.Logger) (*kratos.App, func(), error) {
	discovery := data.NewDiscovery(registry)
	appClient := data.NewAppServiceClient(discovery)
	spuClient := data.NewSpuServiceClient(discovery)
	userClient := data.NewUserServiceClient(discovery)
	dataData, cleanup, err := data.NewData(appClient, spuClient, userClient, logger)
	if err != nil {
		return nil, nil, err
	}
	bannerRepo := data.NewBannerRepo(dataData, logger)
	bannerUsecase := biz.NewBannerUsecase(bannerRepo, logger)
	mallAdmin := service.NewAdmin(bannerUsecase, logger)
	httpServer := server.NewHTTPServer(bootstrap, mallAdmin, logger)
	grpcServer := server.NewGRPCServer(bootstrap, mallAdmin, logger)
	registrar := data.NewRegistrar(registry)
	app := newApp(logger, httpServer, grpcServer, registrar)
	return app, func() {
		cleanup()
	}, nil
}
