// Code generated by Wire. DO NOT EDIT.

//go:generate go run github.com/google/wire/cmd/wire
//go:build !wireinject
// +build !wireinject

package main

import (
	"github.com/go-kratos/kratos/v2"
	"github.com/go-kratos/kratos/v2/log"
	"mall-go/internal/biz"
	"mall-go/internal/conf"
	"mall-go/internal/data"
	"mall-go/internal/server"
	"mall-go/internal/service"
)

// Injectors from wire.go:

// initApp init kratos application.
func initApp(confServer *conf.Server, confData *conf.Data, logger log.Logger) (*kratos.App, func(), error) {
	client := data.NewEntClient(confData, logger)
	dataData, cleanup, err := data.NewData(client, logger)
	if err != nil {
		return nil, nil, err
	}
	bannerRepo := data.NewBannerRepo(dataData, logger)
	bannerUsecase := biz.NewBannerUsecase(bannerRepo, logger)
	themeRepo := data.NewThemeRepo(dataData, logger)
	themeUsecase := biz.NewThemeUsecase(themeRepo, logger)
	activityRepo := data.NewActivityRepo(dataData, logger)
	activityUsecase := biz.NewActivityUsecase(activityRepo, logger)
	appService := service.NewAppService(bannerUsecase, themeUsecase, activityUsecase, logger)
	couponRepo := data.NewCouponRepo(dataData, logger)
	couponUsecase := biz.NewCouponUsecase(couponRepo, logger)
	couponService := service.NewCouponService(couponUsecase, logger)
	orderRepo := data.NewOrderRepo(dataData, logger)
	orderUsecase := biz.NewOrderUsecase(orderRepo, logger)
	orderService := service.NewOrderService(orderUsecase, logger)
	httpServer := server.NewHTTPServer(confServer, appService, couponService, orderService, logger)
	grpcServer := server.NewGRPCServer(confServer, appService, couponService, orderService, logger)
	app := newApp(logger, httpServer, grpcServer)
	return app, func() {
		cleanup()
	}, nil
}
